@model CopyViewModel

@{
    ViewBag.Title = "404 Error";

    
    }

<div class="alert alert-danger mt-1 mb-1">
    <h4>404 Not Found Error : </h4>
    <hr />
    <h5>
        Book copies of book with Id = @Model.Copy.Book.BookId cannot be found
    </h5>
</div>
@*<a asp-action="Details" asp-controller="home" asp-route-id="@Model.Copy.BookId"
   class="btn btn-outline-success" style="width:auto">*@
    <a href="@Url.Action("Rent","BookManagement",new { CopyId = @Model.Copy.CopyId })"
       class="btn btn-outline-success" style="width:auto" m-1">
    Click here to see the Details of the book</a>.